We need to fix the Lambda packaging issue before continuing the refactor.

Current problem:
The Terraform module uses archive_file.source_file, so it only packages data_preparation_job.py.
But data_preparation_job.py imports config.raw_data_set_columns.CSV_COLUMNS, so the config/ directory is not included in the deployment zip.

Task:
Modify the Terraform Lambda packaging so this Lambda can include multiple Python files and subdirectories.

Constraints:
- Preserve the current handler value: data_preparation_job.handler.
- Do not change Lambda runtime behavior.
- Do not rename the Lambda.
- Do not change environment variable names.
- Do not change IAM permissions.
- Do not refactor Python code yet.
- Keep the change as small as possible.
- Prefer supporting both single-file and directory-based Lambda packaging only if the existing module is reused by other Lambdas.
- After changing Terraform, explain whether existing Lambdas using the same module are affected.

Before editing:
1. Inspect all usages of iac/modules/lambda.
2. Determine whether source_file_path is used for only this Lambda or multiple Lambdas.
3. Propose the safest Terraform change.

Then edit only the required Terraform files.

After editing:
1. Show changed files.
2. Show the new packaging behavior.
3. Confirm the zip will include:
   - data_preparation_job.py
   - config/__init__.py
   - config/raw_data_set_columns.py
4. Confirm the handler path remains data_preparation_job.handler.
5. Run terraform fmt.
6. If possible, run terraform plan or explain the exact command I should run.